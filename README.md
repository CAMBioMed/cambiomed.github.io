# CAMBioMed Website

## Requirements

- Hugo (`brew install hugo`)
- Go (`brew install golang`)

## Local Development
To preview the website on a local server:

```shell
hugo server
```

Then open the browser to <http://localhost:1313/>.

